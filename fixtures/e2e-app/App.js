import { Text, View } from "react-native";

import { BUNDLE_MARKER } from "./bundle-marker";

export default function App() {
  return (
    <View>
      <Text>E2E fixture {BUNDLE_MARKER}</Text>
    </View>
  );
}
