# DISABLED — migrated to GitLab CI (../../.gitlab-ci.yml). GitHub is a push-mirror only.
# Kept commented for reference / rollback; this file no longer runs on GitHub.
#
# name: Deploy Server
#
# on:
#   push:
#     tags:
#       - "@better-update/server@*"
#
# concurrency:
#   group: deploy-server
#   cancel-in-progress: false
#
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v6
#
#       - uses: actions/setup-node@v6
#         with:
#           node-version: 22
#
#       - uses: oven-sh/setup-bun@v2
#         with:
#           bun-version: 1.3.13
#
#       - name: Install deps
#         run: bun install --frozen-lockfile
#
#       - name: Apply D1 migrations
#         working-directory: apps/server
#         run: bunx wrangler d1 migrations apply better-update --remote
#         env:
#           CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
#           CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
#
#       - name: Deploy to Cloudflare
#         working-directory: apps/server
#         run: bun run deploy
#         env:
#           CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
#           CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
