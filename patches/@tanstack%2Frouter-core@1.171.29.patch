diff --git a/dist/cjs/load-client.cjs b/dist/cjs/load-client.cjs
index 6284b2353bd2acb96dc6a191f9b7bb6c8648f0e0..5752a6fc7cc0bb0d6038101676c1298eaf1b8129 100644
--- a/dist/cjs/load-client.cjs
+++ b/dist/cjs/load-client.cjs
@@ -719,10 +719,17 @@ function offerPending(router, tx) {
 			}
 			session[2] = 0;
 		}
-		const offered = matches.map((match) => ({
-			...match,
-			_flight: void 0
-		}));
+		const offered = matches.map((match, matchIndex) => {
+			const prior = presented[matchIndex];
+			return {
+				...match,
+				context: prior?.id === match.id && prior.context ? {
+					...prior.context,
+					...match.context
+				} : match.context,
+				_flight: void 0
+			};
+		});
 		offered[index].status = "pending";
 		const ack = session[4] = router.startTransition(() => router.stores.setMatches(offered), offered).then((rendered) => {
 			if (rendered && router._pending === session && session[4] === ack && !session[2]) session[2] = Date.now() + min;
diff --git a/dist/esm/load-client.js b/dist/esm/load-client.js
index 3393414e381935d65e48efc9c681afabf05c4490..6c85476333476a3c1a611eab62a5bf349210604f 100644
--- a/dist/esm/load-client.js
+++ b/dist/esm/load-client.js
@@ -719,10 +719,17 @@ function offerPending(router, tx) {
 			}
 			session[2] = 0;
 		}
-		const offered = matches.map((match) => ({
-			...match,
-			_flight: void 0
-		}));
+		const offered = matches.map((match, matchIndex) => {
+			const prior = presented[matchIndex];
+			return {
+				...match,
+				context: prior?.id === match.id && prior.context ? {
+					...prior.context,
+					...match.context
+				} : match.context,
+				_flight: void 0
+			};
+		});
 		offered[index].status = "pending";
 		const ack = session[4] = router.startTransition(() => router.stores.setMatches(offered), offered).then((rendered) => {
 			if (rendered && router._pending === session && session[4] === ack && !session[2]) session[2] = Date.now() + min;
