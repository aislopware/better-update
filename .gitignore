.wrangler/
lerna-debug.log*
.env.local
.env.*.local
.dev.vars
worker-configuration.d.ts

# Deployment identity + everything rendered from it (`bun run config:gen`).
# Values live in the untracked .env.deploy (or BU_* env), never in git.
.env.deploy
apps/*/wrangler.jsonc
apps/web/src/lib/site-config.generated.ts
apps/cli/src/services/service-defaults.generated.ts
node_modules/
dist/
.turbo/
*.tsbuildinfo
.tanstack
coverage/
__screenshots__/
.vitest-attachments/
test-results/
.e2e-shared-env.json
.self-verify/

# Playwright MCP scratch output
.playwright-mcp/

# Claude Code — share team artifacts (CLAUDE.md, settings.json, commands/, agents/, skills/, hooks/, mcp.json); ignore personal/runtime files
.claude/worktree/
.claude/**/*.local.*
.claude/scheduled_tasks.lock
.claude/cache/
.claude/logs/
.claude/.last-conversation

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

.pi-lens
# CocoIndex Code (ccc)
/.cocoindex_code/
.impeccable/
